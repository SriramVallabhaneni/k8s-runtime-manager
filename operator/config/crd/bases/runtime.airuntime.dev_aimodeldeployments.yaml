---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: aimodeldeployments.runtime.airuntime.dev
spec:
  group: runtime.airuntime.dev
  names:
    kind: AIModelDeployment
    listKind: AIModelDeploymentList
    plural: aimodeldeployments
    singular: aimodeldeployment
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AIModelDeployment is the Schema for the aimodeldeployments API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of AIModelDeployment
            properties:
              model:
                description: Model is the supported Ollama model to deploy.
                minLength: 1
                type: string
              replicas:
                default: 1
                description: |-
                  Replicas is currently restricted to one because each runtime
                  uses a ReadWriteOnce persistent volume.
                format: int32
                maximum: 1
                minimum: 1
                type: integer
              storageSize:
                default: 5Gi
                description: StorageSize controls the requested model-storage capacity.
                type: string
            required:
            - model
            type: object
          status:
            description: status defines the observed state of AIModelDeployment
            properties:
              message:
                description: Message contains additional status information.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration records the resource generation processed
                  by the controller.
                format: int64
                type: integer
              phase:
                description: Phase describes the current runtime state.
                type: string
              ready:
                description: Ready indicates whether the AI runtime is available.
                type: boolean
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
